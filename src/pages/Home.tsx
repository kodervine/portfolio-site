import React from "react";
import DropdownMenu from "../components/DropdownMenu";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <DropdownMenu />
    </div>
  );
};

export default Home;
